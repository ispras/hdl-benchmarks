AG( (m1.REQ_=1) -> AX((m1.REQ_=0) -> AF(m1.GNT_=0)) );

# If REQ_ is asserted, GNT_ is eventually asserted.
# Note: This formula is trivial in our design as there
# is only one master.

# Formula Passed
 
