AG( (m1.FRAME_=1) -> AX( (m1.FRAME_=0) -> AX(m1.OE_AD=0) ) );

# This formula checks for the turnaround cycle necessary in READ
# operations. It uses the OE_AD signal. Compare with 9.ctl which
# uses the TRDY_ signal.

# Formula passed

